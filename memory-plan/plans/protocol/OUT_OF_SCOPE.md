# OUT_OF_SCOPE — protocol plan

Agnostic-spec capture of things observed while working this plan but not acted on (MASTER_PLAN §4.3). WHAT + WHY, never HOW. Reviewed at scope-closing checkpoints: promote, escalate, archive, or defer.

Format per entry: date · area/file · one-line problem · severity guess · next-touch pointer.

---

- **2026-09-06 · repo-wide · operator-requested adversarial + lifecycle review** · A deep adversarial review (7 surface passes + 6 end-to-end lifecycle traces) was run across mesh, memory, Mission Control, install/services, federation/knowledge/viewer, skills, and the governance/release chain. Full graded report at `ADVERSARIAL_REVIEW_2026-09-06.md` (repo root). Highest-severity observations, all verified by reading exact lines (agnostic — WHAT+WHY only): (1) a task `metric`/`description` reaches a `bash -c` sink through a shell filter that does not block the single `&` background operator, from an unvalidated NATS/kanban path; (2) worktrees are git-merged to `main` before the review gate computes; (3) every `GET /api/*` in Mission Control is unauthenticated on a loopback Host and the `memory-file` denylist misses `openclaw.json`/`.env`/session transcripts; (4) `scripts/install/integrations.sh` runs `npx openclaw-mesh`, an unrelated third-party npm package, on the documented install path; (5) CI has concluded failure on `main` for ~3 weeks while status docs claim a green 1550/0 suite, and the mirror workflow republishes each failing push while exposing a PAT on every branch; (6) the only mechanically enforced governance gate is the scope hook, which is bypassable, and the tick's "closed" check trusts a self-written VERSION string; (7) memory promotion counters inflate per-flush and decayed entities resurrect at full salience. · Severity: mix of critical/high · Next touch: operator triage — promote the critical items into a scoped remediation batch; the report's §7 lists the highest-value fixes in order.

- **2026-06-03 ~02:50 ET** · `lib/obsidian-{summarizer,link-checker,decision-notes,theme-notes}.mjs`, `lib/pre-compression-flush.mjs`, `bin/vault-check.mjs` + matching tests · A second headless claude session was editing these repair-plan-domain files concurrently while the protocol plan's steps were closing (tree clean at session start; mtimes 02:47–02:49; two `claude --output-format` PIDs live; no tick locks — so not the chain engine). Meanwhile `plans/repair/` shows BLOCKED.md present and SCOPE idle, i.e. work happening in repair's domain outside an active repair scope. Also observed: `test/obsidian-summarizer.test.mjs` "slug-colliding entities… (repair 2.9)" fails with SQLITE_ERROR under the full suite but passes 18/18 in isolation (twice) — flaky or mid-edit contention. · Severity: medium (scope-discipline + test-stability signal, no data risk) · Next touch: operator — reconcile the parallel session with repair's BLOCKED/idle state.

- **2026-07-03 · deep-review P1** · `mission-control/src/lib/hooks.ts` (`useSchedulerTick`) + `mission-control/src/lib/scheduler.ts` · Task scheduling only runs while a browser tab is open: `useSchedulerTick` POSTs `/api/scheduler/tick` every 30s from the Task Board page only, and there is NO external heartbeat (confirmed: nothing in services/, workspace-bin/, or bin/ calls the tick endpoint). With no tab open, at-triggers and cron tasks never fire; `schedulerStatus().overdue` exists precisely to detect this. The GET-handler mutations were fixed this session (GET /api/tasks and GET /api/scheduler/tick are now read-only), but the browser-dependency itself is unaddressed — the proper fix is an external heartbeat (a launchd/systemd timer curling `POST /api/scheduler/tick` every 30–60s), additive infra touching `services/` + `install.sh`. · Severity: medium (silent scheduler stalls) · Next touch: add a `services/{launchd,systemd}` heartbeat unit + install.sh wiring, guarded on the MC server being up.

- **2026-07-03 · ctx analysis (interactive session)** · `lib/mcp-knowledge/server.mjs`, `lib/extraction-store.mjs`, `lib/retrieval-pipeline.mjs` · Verified analysis of ctxrs/ctx (Apache-2.0, clone + line-level audit) found two adoptable capability gaps: (1) no read-only SQL surface for agents — ctx's MCP `sql` tool proves the pattern (read-only open + `PRAGMA query_only` + single-statement + `stmt.readonly` check + row/byte caps) and we could expose state.db/knowledge.db/mission-control.db the same way via mcp-knowledge; (2) `decisions` has no FTS — `themeEntitySearch` still runs unindexed per-theme LIKE scans (the F-H23 stopgap), fixable with a contentless-content `decisions_fts` table that also becomes a retrieval channel. Side note for Phase G: ctx's `sync_outbox` schema (UNIQUE(local_table,local_id,operation), `withheld` state, attempt/backoff cols) is a good reference for the promotion path — but it is schema-only in ctx, no runtime exists; borrow the drawing, not a claimed implementation. Full step-level path delivered in the 2026-07-03 session transcript. · Severity: low (capability gap, no defect) · Next touch: operator — triage into a scope addendum or new silo when the current P0/P1 scope closes.

- **2026-07-03 · deep-review P1 (PARTIAL FIX)** · `mission-control/src/app/api/diagnostics/test-runner/route.ts` · The diagnostics test-runner calls the real `schedulerTick()` six times (can dispatch real user tasks + fire real gateway notifications for a test task it then deletes) and writes `__TEST__` rows into the live `active-tasks.md`. **Fixed 2026-07-03:** the `LIKE '__TEST__%'` wildcard footgun (`_` matches any char) → `GLOB '__TEST__*'` (literal), and a `syncTasksToMarkdown` re-sync after DB cleanup so `__TEST__` rows no longer linger in the live markdown. **STILL OPEN:** the real `schedulerTick()` calls against the production DB — a `dryRun` is wrong here because the diagnostics must observe *real* dispatch, so the correct fix is DB isolation (a throwaway DB + scheduler context injected into the runner), a larger refactor of the 990-line route not safely doable blind. · Severity: medium→low (external-notify/dispatch during a diagnostics run remains; markdown residue + wildcard closed) · Next touch: inject a `{ db, scheduler }` context so the runner operates on a throwaway DB.

- **2026-07-03 · deep-review C2 (exec half — NOT fixable in this repo)** · `bin/mesh.js` (`openclaw.<node>.exec` publisher) + `lib/exec-safety.js` · The deploy-trigger half of C2 is now closed (opt-in signed deploys, `lib/deploy-trigger-auth.mjs`). The exec half is not fixable here: `bin/mesh.js` PUBLISHES to `openclaw.<node>.exec` and runs the destructive-command blocklist CLIENT-side before publishing, but there is NO exec responder/subscriber anywhere in this repo (grep of bin/ for a subscriber of `openclaw.*.exec` / a msg.respond exec handler found none). The executor lives in remote-node code outside this tree (or nowhere). So "move exec safety server-side" has no server here to harden, and the blocklist is bypassable by a publisher who simply doesn't run the CLI filter. · Severity: high IF the remote executor exists and trusts the subject (NATS-publish = RCE); UNKNOWN because the executor isn't in-repo · Next touch: locate the exec responder (remote repo), replace its blocklist with an allowlist enforced server-side, and require a signed+trusted exec envelope (reuse `lib/deploy-trigger-auth.mjs` / `node-identity.mjs`).

- **2026-07-16 · vault-frontier batch (interactive session)** · `~/.openclaw/config/obsidian-sync.json` · The workspace→arcane-vault sync leg is OFF on this node: `enabled: false` and empty `memoryVaultPath`, so the 2026-07-04j fusion is not in effect here — the memory pipeline writes the legacy `~/.openclaw/obsidian-local` and nothing pushes workspace artifacts into the real vault (workspace `projects/arcane-vault/` tree carries March mtimes). Unclear whether this is a deliberate per-node choice (VM vs main node) or config drift after fusion. · Severity: medium (whole sync surface dormant on this node) · Next touch: operator decides intended state; if fusion is wanted here, set `memoryVaultPath` + `enabled: true` and re-run the migration check from addendum 2026-07-04j.

- **2026-07-16 · claude-obsidian comparison (interactive session)** · `lib/memory-inject-server.mjs` + future `lib/` module · Two comparison takeaways deliberately not taken in the vault-frontier batch: (1) PostCompact re-injection — claude-obsidian re-injects its hot summary on the `PostCompact` hook so working memory survives compaction; verify whether our injection surface covers post-compaction context loss and add the hook if not. (2) Embedding-based near-duplicate concept detection (their "tiling": pairwise cosine over local-Ollama embeddings, report-only) — would catch the live extraction noise observed 2026-07-16 (`clade` / `clade-code` / `claude-code` as separate concepts) and complements the descoped slug-collision dedup (repair OUT_OF_SCOPE). · Severity: low (capability gaps, no defect) · Next touch: operator triage into a future scope addendum.

- **2026-08-02 · `bin/consolidation-scheduler.mjs` idle gate — CLOSED v4.1** · The standalone scheduler treated a model listed by Ollama `/api/ps` with nonzero VRAM as active inference even when the internal queue reported no current or pending job; the log showed one hard-cap failure followed by 359 false-busy skips. Step 4.1 replaced residency with fresh daemon queue-state evidence and observed a real cycle start. · Severity: closed · Next touch: none for the idle gate.

- **2026-08-02 · consolidation/local-event NATS clients — CLOSED v4.1** · The scheduler could not authenticate to the token-gated bus and continued without event emission. Step 4.1 routed standalone clients through the shared token resolver; the deployed scheduler and daemon both connected and the network watcher observed the stream. · Severity: closed · Next touch: none.

- **2026-08-02 · local event stream naming — CLOSED v4.1** · The live watcher derived a stream name from the unsanitized hostname `MoltyMacs-Virtual-Machine.local`; NATS rejected the dots. Step 4.1 made one canonical helper identity-preserving for deployed ids and mapped the dotted hostname to `moltymacs-virtual-machine`; the host network axis now grades `net.stream` WORKING. · Severity: closed · Next touch: none.

- **2026-08-02 · consolidation cycle duration after v4.1 gate repair** · The first queue-authorized launchd cycle started correctly and emitted through NATS, then exceeded the existing 300000ms hard cap at 300029ms. Scheduled liveness is restored, but daily digest/vault cadence is not proven restored until a real cycle completes within a measured or redesigned budget. · Severity: high (memory synthesis remains degraded) · Next touch: profile phase timings and bound/partition the expensive stage in a separately approved consolidation-performance step; do not merely raise the cap without evidence.

- **2026-08-02 · scheduler heartbeat launchd unit — CLOSED v4.4** · The unit now invokes an installer-owned loopback-only helper that reads the existing Mission Control token internally. Bare POST remains HTTP 401; authenticated launchd recurrences return HTTP 200 and last exit 0. · Severity: closed · Next touch: none.

- **2026-08-02 · `lib/mcp-knowledge` standalone dependency tree — CLOSED v4.2** · The two nested 0.34.5/libvips trees and independent lockfile are gone; mcp-knowledge is root-workspace-owned and all deployed imports resolve Sharp 0.35.3/libvips 8.18.3. Duplicate native state was a proven security/process risk but not a proven cause of the teardown abort; embedding isolation now contains that still-unidentified native interaction. · Severity: closed · Next touch: none for dependency topology; any future native root-cause work starts from the v4.2 audit.

- **2026-08-02 · node-watch process truth — CLOSED v4.3** · Gateway now requires a running PID plus a session JSONL fresher than 24h, and mesh/coordinator/core launchd verdicts parse `launchctl print` for positive PIDs. The deployed daemon no longer greens the stale gateway artifact or PID-less mesh labels. Stack output's duplicate stale-unit presentation remains separate from watcher grading. · Severity: closed · Next touch: none for watcher verdict semantics.

- **2026-08-02 · service failures surfaced by v4.3** · Gateway alternated between `spawn scheduled` with no PID and a short-lived PID while its newest session remained 69,773 minutes stale; mesh-agent and mesh-tool-discord are loaded without PIDs. The watcher now reports these states rather than hiding them. · Severity: high for gateway/mesh-agent readiness, low for optional mesh-tool-discord · Next touch: gateway runtime diagnosis; mesh-agent must be running with a D11 worker before federation 2.6/3.5 evidence; confirm whether mesh-tool-discord should be unloaded or started for this node role.

- **2026-08-02 · retired single-node NATS launchd artifact** · `ai.openclaw.nats` remains loaded in a 1,448-run spawn loop with last exit 1 while the real `ai.openclaw.nats-{1,2,3}` R=3 services all run with PIDs. Step 4.3 stopped treating the retired label as the active core bus, but the stale unit still pollutes stack/service inventories. · Severity: low (operator ambiguity and needless launch attempts; live cluster unaffected) · Next touch: service-manifest/installer cleanup should retire the single-node unit when the R=3 topology is selected.

- **2026-08-02 · `bin/hyperagent.mjs` operator text** · CLI help omits the implemented `report --run` command, and the reflection command still prints that a pending synthesis will be picked up by a retired harness rule. · Severity: low (operator guidance contradicts D1/D3) · Next touch: HyperAgent CLI/docs maintenance after governance.

- **2026-08-02 · repair plan viewer/automation conformance** · The completed v7.8 repair ledger is coherent, but `plan-lint.sh repair` reports 3 FAILs because the pre-protocol silo has no `automation.json`, `TICK_PROMPT.md`, or `tick-logs/`. This does not reopen its 49 implementation steps; it means its Automation/History surfaces are incomplete. · Severity: low (archived control-plane debt) · Next touch: protocol conformance-retrofit scope, not runtime repair.

- **2026-08-02 · npm package surface** · `npm pack --dry-run --json` succeeds but reports a 375.7 MB archive, 1.015 GB unpacked, and 42,613 entries because generated/runtime trees such as `mission-control/.next` are included. The v4.2 files are packaged correctly and the removed child lock is absent, but release artifacts are unnecessarily large. · Severity: medium (distribution cost and accidental-surface risk) · Next touch: packaging scope adding an explicit `files` manifest or `.npmignore`, followed by an artifact-content contract test.
