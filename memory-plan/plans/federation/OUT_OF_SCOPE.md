# OUT_OF_SCOPE — federation plan

Agnostic-spec capture of things observed while working this plan but not acted on (MASTER_PLAN
§4.3). WHAT + WHY, never HOW — no prescribed solution, no code excerpts. Always-writeable
regardless of scope. Reviewed at scope-closing checkpoints: each entry gets promoted into
SCOPE.md, escalated into INVENTORY.md, archived as won't-fix, or deferred forward.

Format per entry: date · area/file · one-line problem · severity guess · next-touch pointer.

---

- 2026-07-09 · INVENTORY.md:25-26,38,48 + GRANULAR_PHASE1.md:34 · step 0.1/1.2 contracts reference "DECISIONS D2" as the future crash-loop triage table, but D2 was consumed by the 2026-07-06 review decision (NATS trust floor) — the ledger id is taken, so 0.1's Verify contract points at the wrong entry · medium (a tick executing 0.1 would mis-file/mis-verify) · fix before 0.1 starts.
- 2026-07-09 · services/service-manifest.json + install.sh:794 · nats-{1,2,3} are absent from the service manifest and their plists live in services/nats/ while install.sh renders only services/launchd/ — the cluster units are orphaned from the install path, and g.1.1.d's "confirm all three are in the manifest" is a confirmation that is false today (they must be added, and the render loop doesn't reach them) · medium (step 1.1 task encodes a false precondition) · step 1.1.
- 2026-07-09 · GRANULAR_PHASE1.md T1.1.1 + DECISIONS D2 wording · "install.sh provisions OPENCLAW_NATS_TOKEN that nothing consumes" is imprecise: lib/nats-resolve.js:70,77,87 resolves it and passes opts.token client-side for every bin/ client — the real gap is server-side only (no authorization block in the .conf files). Security conclusion unchanged; the client half of g.1.1.h already exists · low (doc precision; D2 is append-only so needs a superseding note) · step 1.1 design phase.
- 2026-07-09 · plan naming vs lib/federation-startup.mjs, lib/federation-resilience.mjs, broadcast-emitter/offerer/acceptor, openclaw-status.mjs · the word "federation" is already occupied in code by the redesign's memory-broadcast layer (wired into the memory daemon; openclaw-status "federation wiring" means THAT layer) — the plan's fed.* probe family and plan name create a two-subsystems-one-name ops hazard, and the GRANULAR reality-anchor table omits this seventh neighbor · low-medium · 0.2 FEDERATION_SPEC naming section.
- 2026-07-09 · test-count citations · D1 + INVENTORY 2.1/3.1 say "40 tests"; g.2.2.b says "the existing 27" for collab-circling.test.js; actual top-level it() counts: collab-circling 31, daemon-circling-handlers 13 (their sum 44 matches g.3.1.c), circling-comprehensive 49 — the 40/27 constants match no current file, and 3.1's Verify contract cites "40 still green" · cosmetic (but it sits in a Verify contract) · 2.1/3.1 design.
- 2026-07-09 · install.sh:1064-1082 + openclaw-mesh prototype (operator-provided zip, github.com/moltyguibros-design/openclaw-mesh) · install.sh still delegates its mesh phase to `npx openclaw-mesh` whenever Tailscale is present and no mesh CLI exists — a fresh node gets the prototype fleet stack (com.openclaw.agent LaunchDaemon, remote-exec agent with no server-side blocklist, tailnet-trust NATS model) beside the manifest services, while uninstall.sh:77-80 classifies that same daemon as legacy; the exec channel is repo-audit finding #84 (any tailnet device can publish openclaw.{node}.exec), and repo bin/mesh.js is an evolved copy speaking the openclaw.* namespace (audit #197's predicted two-repo drift is now real, 957 diff lines) — the fleet layer needs one story: absorb into Block 6/7 or retire · high · 6.1 install profile + 0.2 subject-map.
- 2026-07-09 · step 1.1 vs live runtime · INVENTORY 1.1's Needs "ports 4222-4224 free" is false on the lead node: 127.0.0.1:4222 is held by the LIVE single-node ai.openclaw.nats (PID 1989 observed 2026-07-09) whose JetStream carries production data (local-events-daedalus: 14,364 msgs, last message minutes old, plus the MESH_* KV buckets) — 1.1 is a live-bus cutover with data migration, not a green-field bring-up · high (planning) · 1.1 design phase.
- 2026-07-09 · /Library/LaunchDaemons/com.openclaw.agent.plist · loaded in the SYSTEM domain (`launchctl print system/…`: state "spawn scheduled", KeepAlive, active count 0; process absent, ~/openclaw workdir absent, declared /tmp logs absent) — an armed-but-inert zombie unit outside step 0.1's LaunchAgent-only inventory (the 11 .disabled units); the system domain must be swept too, and a reboot could revive it if paths reappear · medium · 0.1.
- 2026-07-09 · cluster-config lineage · the 0.0.0.0/no-auth posture of services/nats/nats-{1,2,3}.conf descends from the prototype's DOCUMENTED security model (openclaw-mesh SKILL.md §Security: "Tailscale is the authentication layer — no tokens, no TLS"), i.e. a deliberate prior decision, not an omission; D2's loopback+token stance supersedes it, and repo-audit finding #84 is the evidence why — record the supersession context so the tailnet-trust model doesn't resurface via the npx install path · low · D2 addendum / 0.2.

**Triage 2026-07-09 (operator-approved absorb-and-retire):** all nine entries above promoted in the D4 doc pass — DECISIONS D4 recorded; INVENTORY 0.1 (system domain), 1.1 (live-bus cutover), 1.2, 2.1, 3.1 (test counts), 6.1 (retirement contract), 7.4 [D] (absorbed capabilities); GRANULAR anchors + g.0.1.a/d + T1.1.1 wording + g.1.1.cut; PHASE1_TASKS T0.1.1/T0.1.4/T1.1.3/T1.1.7/T1.2.2. None remain open.

- 2026-07-10 · services/nats/ai.openclaw.nats-{1,2,3}.plist · original plist templates in `services/nats/` are superseded by the new `services/launchd/` templates (step 1.1) — the nats/ copies use `${OPENCLAW_REPO}` (wrong variable) and reference the repo conf directly (not the rendered path); they are now dead code that install.sh never reads · low · cleanup, any future session.

- 2026-07-09 · step 1.1 contract + TICK_PROMPT (chain safety) · the autonomous chain, reaching 1.1, did NOT block on the missing "cutover plan" Need — it read that Need as "design the cutover in AUDIT_PRE, then execute in Phase 4" and advanced to v1.1-pre with a Phase-4 plan to `launchctl unload` the live :4222 bus and migrate 14k JetStream msgs. A launchd safety interlock hard-paused the job at Phase 1, before any bus action (bus verified intact: PID 1989, 14,552 msgs). The incomplete 1.1 Phase-1 work was reverted to clean v0.2. WHY it matters: a production-data migration must be operator-gated, and 1.1 as written lets a capable headless tick design AND execute it unattended — a "cutover plan" Need is not a hard gate · **HIGH** · before re-enabling the chain, gate 1.1 so the cutover cannot run headless: split the non-destructive cluster-prep from the operator-gated cutover, OR add a `visual:`/operator-signoff Verify that forces a headless BLOCK. The tick's own migration design was sound and worth reusing when 1.1 is done deliberately: `nats stream backup` each stream/KV → keep single-node up until restore verified → bring the R=3 cluster up → restore → verify message counts match → only then retire the single-node unit.

- 2026-07-10 · bin/mesh-{task-daemon,bridge,agent,health-publisher}.js + spawn-node.mjs (parallel audit ×2 — the daemons step 1.2 revives) · the revived daemons carry REAL latent runtime bugs, never caught because they only ever crash-looped on a dead path (D5 "code health unobservable"). **Blocks 1.2's own verify:** mesh-agent reads `MESH_NODE_ID`, mesh-health-publisher reads `OPENCLAW_NODE_ID`, and spawn-node.mjs's `node.json` (id/nats_url) is consumed by NEITHER → three co-located nodes collapse to ONE host id → 1.2's "3 distinct node-ids heartbeating" verify FAILS unless the launcher exports both id vars + OPENCLAW_NATS + OPENCLAW_HOME per node; only the publisher writes the MESH_NODE_HEALTH bucket MC reads, so the agent alone proves nothing. **Must-fix before 1.5** (its cutover restarts the bus): mesh-task-daemon.js:2094 has NO reconnect/disconnect handling (unlike mesh-bridge) → does not survive a bus restart, fails SILENTLY (no process.exit → launchd won't relaunch); every nc.publish (incl. handleCirclingStepTimeout:1310, uncaught) throws synchronously in a disconnect window → unhandledRejection → death; mesh-agent maxReconnectAttempts:10 (~20s) dies on any longer outage while the publisher (−1) survives; health-publisher bucket-open (342) unwrapped → crashes on TTL mismatch with the existing bucket. **Must-fix before 2.1** (live circling): mesh-collab.js:1154 evaluateRound is a non-CAS blind-write → lost-update clobbers concurrent reflections → session hangs; handleCollabReflect:932 TOCTOU double-advances the circling step; malformed-artifact reflection:905 throws leaving the barrier un-completeable. Plus ~10 lower (unbounded KV/maps never pruned, O(N) 5s bucket scans, non-atomic active-tasks.md writes). · **HIGH** · 1.2 launcher env fixes are the immediate gate; task-daemon reconnect before 1.5; collab races before 2.1.

- 2026-07-10 · docs/FEDERATION_SPEC.md (0.2's autonomous output — adversarial review) · sound on the EXISTING circling flow (every file:line anchor checks out) but written against an IMAGINED interface in load-bearing places that will stop Block 3-5 code at first integration: **F1** envelopes timestamp with `issued_at` but signEvent/verifyEvent freshness keys on `timestamp` (node-identity.mjs:424) → every envelope fails verification 100%; spec:88 falsely says signEvent adds `event_id` (it adds only signature+signer_pubkey — the caller injects timestamp+event_id, per deploy-trigger-auth.mjs). **F2** no envelope carries `node_id` → the registry impersonation-defense (verifyEvent expectedNodeId, node-identity.mjs:527) can't run → a worker could forge a result as another grappe's coordinator. **F4** spec invents `session.architecture` (§3) AND `session.type` (§4.1) as the mode discriminator — neither exists; real is `session.mode` (mesh-collab.js:59) → B/C/mgmt sessions fall through. **F6** grappe manifest stores bare node_ids + join_token_hash, NO member pubkeys/sig → "signed membership (ed25519)" has nowhere to live and can't seed the registry (blocks F2). **F3** §2.3 conflates HMAC join-tokens with ed25519 signEvent as "the same pair" (unrelated), and token integrity is UNVERIFIED on the consume side. **F5** §7 "no openclaw.* after 6.1" would rip out lib/tracer.js's openclaw.trace.* spine (35 modules) — carve it out. **F7** §2.1 "override 0.0.0.0" is stale (configs already loopback+token; real gap is only the unrendered token placeholder). **F8** cites DECISIONS.md/ROADMAP.md/D1-D5 by bare name — they live in the plan silo, not repo-root/docs (navigability). · **HIGH** · correct F1/F2/F4/F6 before Block 3; they feed 1.4 signing + 3.1 dispatch directly.

- 2026-07-11 (evening) · **FRESH-INSTALL DEPLOYABILITY AUDIT** (4 parallel auditors + Opus cross-review + direct verification; supersedes-and-deepens the entry below; full evidence in the 2026-07-11 session "fresh-install review") · install.sh + services/* + workspace-bin/memory-daemon.mjs + README/MULTI_NODE_DEPLOY.md · **A clean `git clone && bash install.sh` yields a node where exactly TWO subsystems work: notifications (ledger+popups) and node-watch (which then honestly grades the rest BROKEN/OFF/UNKNOWN).** Default run starts nothing (`--enable-services` gates every load, install.sh:32,872,933); with the flag, ~5 units hot crash-loop + 2 error-loop. Verified killers: **(1) memory-daemon dead at ESM import — THREE independent kills:** memory-daemon.mjs:53 imports lib/mcp-knowledge/core.mjs which install.sh:345 `--exclude`s from the workspace lib copy (while :385-388 npm-installs that pkg's deps into the OTHER copy at ~/openclaw/lib that nothing imports); :54 imports ../bin/obsidian-graph-cache.mjs which the 3-file cherry-pick (install.sh:337) never places (regression vector: v5.2 463a76d added the static import after the copy rules were written); lib/shared-event-stream.mjs:20 + lib/local-event-log.mjs:37 top-level require('nats') while $WORKSPACE/node_modules gets only 4 native symlinks (install.sh:367-371). **(2) No NATS bus ever:** nats-1/2/3 autostart:false (manifest:3-16) so never loaded even with the flag; nats-server binary never installed (plists hardcode /opt/homebrew, wrong on Intel/Linux); ZERO systemd NATS units; suspected store_dir tilde-expansion fault (nats-1.conf:16, launchd cwd /); meanwhile task-daemon/bridge/health-publisher are autostart:true with naked `await connect()` → exit(1) → KeepAlive relaunch every 5-10s (mesh-task-daemon.js:2292-2295, mesh-bridge.js:899-901, mesh-health-publisher.js:409). **(3) Repo deps never installed:** no root `npm install` in install.sh or README:30-33 — macOS mesh units exec from the clone with no `nats` module (the npx-cache path masks this; the documented git-clone path is dead); mesh-deploy-listener/tool-discord can't resolve nats from ~/openclaw either. **(4) MC double-broken:** exec `~/.openclaw/bin/npm` never created (no line writes it) + `next build` never run and can't pass (tsc --noEmit = 20 errors, observed this audit). **(5) gateway execs global `openclaw` pkg that install never installs; observer (autostart:true) execs $WORKSPACE/bin/observer.mjs never placed; log-rotate execs a script that exists nowhere in the repo; mesh-tool-discord exit(1)s on missing token. **(6) LLM unprovisioned on every layer:** agents default claude CLI/sonnet (llm-providers.js:219-240) — Gap A confirmed, zero MESH_LLM_PROVIDER/LLM_MODEL anywhere in install/env/units (only bin/openclaw-node-init.js:446,540 writes them, and install never calls it — two half-installers); extraction is local-by-design (qwen3:8b, llm-client.mjs:32-40) but zero ollama install/pull → **silent regex degradation** (pre-compression-flush.mjs:577-585); embeddings are Xenova/bge-m3 via transformers.js (NOT ollama), lazy ~2GB HF download, hard error offline; **consolidation never passes an LLM client on ANY machine** (consolidation-scheduler.mjs:175-183 → consolidate.mjs:243-246 omit client) — latent, not fresh-only. **(7) Identity/trust never provisioned:** no ed25519 keygen/join-token at install (its "identity" step copies persona markdown, install.sh:394-408) though node-identity.mjs/mesh-join-token.js exist and MULTI_NODE_DEPLOY.md:122-145 promises it. **(8) The doc is a trap:** MULTI_NODE_DEPLOY.md:69-71 starts NATS from UNRENDERED template confs → server token = literal `${OPENCLAW_NATS_TOKEN}` vs clients' generated hex (install.sh:475-482,538-540) → auth mismatch by following instructions; 9 doc-drift items total incl. env knobs (NATS_URL/OPENCLAW_HOME) the shipped daemon never reads. **(9) The June deploy gate bin/node-acceptance.mjs is copied (install.sh:337) but NEVER executed by install — it ends in an echo banner, zero verification (install.sh:1314-1347).** **(10) Hygiene:** com.openclaw.redesign-tick.plist ships hardcoded /Users/moltymac paths; daedalus-hardcoded orphan plists (consolidation-scheduler:34, health-watch:133) not in the manifest; `--update` without `--enable-services` unloads every unit (:870) and never reloads — downs a live node; `--dry-run` not side-effect-free (unguarded writes into never-mkdir'd dirs). Also structural: the unit runs the OLD workspace memory-daemon while the federation-hardened bin/openclaw-memory-daemon.mjs has NO unit (two-parallel-daemons echo); grappe registry is write-only metadata — no consumer maps grappe.mode to session behavior (circling runs off task.collaboration; "adversarial" appears nowhere in mesh-collab.js, real mode string is circling_strategy). Two ~10-step verified manual runbooks exist (session record): (a) fresh box → working memory harness; (b) fresh box → mock circling session (shell provider). · **CRITICAL (node-level — wider than federation)** · Triage with the entry below's actions (1)-(3), which this audit evidences: P0 = memory-daemon copy/symlink fixes + single-node NATS provisioning decision + repo npm install + **wire node-acceptance as install.sh's final fail-loud phase** (the missing forcing function); P1 = LLM env wiring + ollama pull + identity keygen + dead-exec fixes + connect-retry hygiene; P2 = Linux parity, doc rewrite, cruft removal, dry-run/update honesty, consolidation LLM wiring.

- 2026-07-12 · mission-control/src/app/api/mesh/nodes/route.ts:330 · the node-payload builder is cast `as any`, which is exactly what let its `NodeHealth` interface drift undetected from the payload (the 21-tsc-error family fixed 2026-07-12 traces to it) — the same drift will recur until the cast is removed and the builder is typed against the interface · low-medium · MC maintenance, any future scope.

- 2026-07-14 · **MEMORY-SYSTEM DEEP-REVIEW — deferred P4 items** · lib/memory-budget.mjs + install.sh + memory-injector · Landed this pass: P0 (regex fallback can't corrupt the structured MEMORY.md + memory.error on degradation), P1 (worker task-relevant recall via inject-server + fallback, all 4 builders), P2 (relationships persisted + fed to the concept graph; decisions age by created_at), P3 (privacy filter → session-grain, dead turn-grain branch removed). DEFERRED as too-risky/unverifiable-here: (a) **workspace runtime scripts are diverged COPIES not symlinks** of the repo (obsidian-sync, session-recap, subagent-audit, phase-2) → drift; fix = install.sh symlink instead of cp (deploy change, verify on a real install). (b) **MemoryBudget.addEntry is a latent 2nd MEMORY.md writer** alongside generateMemoryContent; used by bin/openclaw-status.mjs + workspace-bin/memory-daemon.mjs so NOT clearly dead — audit callers, then unify on one writer. (c) **recallScore graph_activation still inert** (never populated); now that P2 concept_edges exist, wire spreading-activation to set item.graph_activation. · low-medium · (a) next real install; (b)/(c) a focused memory pass.

- 2026-07-14 · **RUNTIME (memory) — observed this session** · ~/.openclaw/workspace/MEMORY.md + memory-inject-server · (1) the live MEMORY.md is CORRUPTED with `[assistant]/[user]` regex fragments (~lines 28-36) — self-heals on the next successful LLM flush (regenerated from SQLite); P0 prevents recurrence. (2) the **memory-inject-server (:7893) has been DOWN since 2026-07-10** (last "listening" log) → the MAIN agent's proactive memory injection is not firing, and P1 worker recall falls back to the index. · medium · restart the daemon's inject-server (check why it died) + add a node-watch probe for :7893; optionally clean the live MEMORY.md now.

**Triage 2026-07-11 evening (operator-directed):** the audit entry above is substantially CLOSED by the `deployability-install-overhaul` batch (DECISIONS D9, `audits/deployability_overhaul/AUDIT.md`): P0 (five memory-daemon boot kills, single-node bus unit, repo deps, render audit, acceptance gate as install finale) + P1 (LLM env wiring + tiered pull + BGE prefetch, ed25519 identity, dead-exec fixes, unload fix, systemd exec-root unification, honest demotions) delivered and sandbox-verified. STILL OPEN: MC production build (20 tsc errors — gate reports it honestly), consolidation LLM wiring (latent, every machine), `--dry-run` honesty, full MULTI_NODE_DEPLOY rewrite, mesh-daemon waitOnFirstConnect hygiene (KeepAlive relaunch now converges once the bus unit is up, so demoted to P2), and the **T7 clean-machine gate** (now the reworded step 6.1).

- 2026-07-16 · **DEEP REVIEW — federation plan, code, runtime** (six-source: full silo-doc + DECISIONS read, four parallel audit agents [Block-3 code · fed-probe honesty · live-runtime ground truth · close-evidence quality], targeted verification; all read-only; concurrent session committed e6b05bb mid-review). The full report was delivered in the review session chat; captured here agnostically. **Overall:** the choreography (modes/barriers/KV state machines/cluster) is real and runs, and the honest-status vocabulary is genuinely in use — but the 07-15 close batch bought velocity by softening the plan's hard gates while the easy steps stayed clean (the early-May signature, not yet May redux). The entries below decompose it; each is WHAT+WHY, next-touch tagged.

  - 2026-07-16 · **RUNTIME vs CLAIMED STATE — several closed/claimed states are false against the live box (probed 07-15 23:55–00:05 EDT).** WHY it matters: this plan's founding lesson (MASTER_PLAN §5, §4.1) is that commit≠delivery; the drift is exactly clause 2–4 (deployed/restarted/observed) being skipped. Observed mismatches: (1) a real 3-node `openclaw-cluster` has been live since **Jul 14 19:41 EDT** (deliberate manual cutover: configs 19:11→plists 19:31→procs 19:41) while step 1.5 cutover is still `[D]`, COMPONENT_REGISTRY still says "single node/cluster=NONE" (07-10), and no step/DECISION/registry-update records it; old single-node unit still loaded+dead. (2) The R=1→R=3 KV migration the step15 audit says happened "earlier this session" is a production-bus mutation of the class 1.5's ⛔ gate reserves for the operator, done with no numbers/audit/decision; `GRAPPE_REGISTRY` was recreated 07-15 18:48 and is now EMPTY (wg-alpha manifest gone). (3) The old bus's production data — `local-events-daedalus` 14k+ msgs — is ABSENT from the cluster; `OPENCLAW_SHARED` has 0 msgs since creation; the stranded store's disposition was never declared. (4) Block 1 shows all `[x]` but the grappe substrate is COLD: alpha/bravo/charlie publishers dead since Jul 10 08:09, `MESH_NODE_HEALTH` holds only the main-node key, mesh-agent unit never ran since load — no grappe exists at runtime. (5) Mission Control is DOWN/crash-looping (no :3000 listener; "Could not find a production build in .next" ×9,270; dev artifacts only) despite SCOPE 6.2 "runtime-verified live"; `scheduler-heartbeat` fails every 60s as collateral. (6) The live node-watch process (started Jul 14 19:41) PREDATES the fed-probe code (Jul 15 19:07) and was never restarted → zero fed.* grades in any live snapshot, so 6.3's "observed honest grading" close has never run in the running watcher. (7) 19 commits unpushed — CI has never seen this week's work; the whole recent history lives on one machine, in a plan about machine-loss failsafe. (8) `~/.openclaw/openclaw.env` carries TWO different `OPENCLAW_NATS_TOKEN` lines (one live, one rejected) — prime suspect for the recurring "NATS client auth" findings. (9) `npm test` = 1801/1803, one deterministic failure test/observer.test.mjs:36 (idle-window; test pins now=2026-07-04 — possibly date-rollover-sensitive). · **HIGH (truth-reconciliation)** · a reconciliation pass before any new step: restart node-watch, fix/demote the MC unit, dedupe the token, re-probe + update COMPONENT_REGISTRY, and write a DECISIONS entry (proposed D12) recording the cutover + migration + the `[D]`-row enabler.

  - 2026-07-16 · **MEMORY-PIPELINE may have gone partially dark during the federation sprint** · `mem.ingest` newest message Jul 14 23:04Z (~37 min before cluster start), nothing ingested in ~29h across two heavy workdays; `OPENCLAW_SHARED` unwritten since cutover; inject-server :7893 already flagged down since 07-10 (prior OUT_OF_SCOPE entry). WHY: the core product of this repo is the memory harness; a federation cutover that silently stopped ingest/shared-event flow would be a severe regression hiding behind green federation steps. · **HIGH** · ~10 min of verification (dup-token is suspect #1 — if the daemon sources the dead token it can't reach the bus); confirm ingest + shared-event write + :7893 are alive, independent of federation.

  - 2026-07-16 · **fed.* probe honesty defects — green states that are structurally unearnable / unfalsifiable** (lib/node-watch.mjs, lib/fed-probes.mjs, lib/node-acceptance-probes.mjs). WHY: this family IS the honesty layer (MASTER_PLAN memory: never WORKING without an observed signal); these defects let it show green with no live signal for the property named. (a) **P0** `fed.cluster.quorum`/`FED-L2-QUORUM` counts self twice — `membersUp = 1 + min(routes, connect_urls.length)` and NATS `connect_urls` includes the local server (verified live) → both peers dead still reads "quorum held 2/3 up" WORKING/PASS; the authoritative `jsz.meta_cluster` signal is fetched then discarded; 6.4's "quorum honesty fix" corrected only the happy-state readout; the new multi-machine template lacks `no_advertise` so this ships into the cross-machine rollout. (b) **P1** varz-unobservable silently degrades to "single-node bus up" WORKING (same class as the `.body` bug 6.4 fixed). (c) **P1** `fed.grappe.members` reads heartbeat fields (`timestamp`/`updated_at`) no writer produces (publisher writes `reportedAt`) → any live grappe grades BROKEN; WORKING branch unreachable against production data. (d) **P2** coordinator probe grades WORKING from launchd *load* state — a loaded-dead unit counts as up (pattern live on this box). (e) **P2** the "read-only" watcher CREATES KV buckets as a probe side effect (`views.kv()` without bindOnly → Bucket.create) — likely how the empty GRAPPE_REGISTRY reappeared. (f) **P2** KV-read failure grades BROKEN, contradicting the module's own "UNKNOWN when unobservable" header. (g) **P3** probes hardcode 127.0.0.1:4222/:8222, bypassing the repo's nats-resolve chain. · **HIGH** · these invalidate 6.3/6.4 close evidence in their degraded branches; fix before the Phase-1 gate leans on them.

  - 2026-07-16 · **Block-3 cooperative/collaborative modes — every degradation path silently completes with placeholder garbage or deadlocks** (bin/mesh-task-daemon.js, lib/mesh-collab.js, bin/mesh-agent.js, lib/llm-providers.js). WHY: the commits advertise fail-loud dispatch + real vote/merge machinery; the degraded paths do the opposite, and none is tested (tests reimplement daemon math inline / use the auto-voting shell provider, so the vote/convergence/parse machinery of the new modes has never been observed live). (a) **P0** the join-time recruiting-close path (mesh-task-daemon.js ~770-788) kept the pre-3.1 binary dispatch → under the natural config (3-node grappe, max_nodes:3, third join closes recruiting before the sweep) cooperative starts with empty integrator rotation and "completes successfully" on placeholder rounds; collaborative gets no decomposition/merger; management silently runs the legacy protocol — the exact silent-wrong-protocol failure 3.1 claims to have closed. (b) **P1** merge-review votes are decorative — two `blocked` complete identically to two `converged`. (c) **P1** no barrier timeout for the new modes (timers/rehydration are circling-only; collab tasks have no owner/deadline so stall+budget backstops never fire) → one crashed member blocks forever. (d) **P1** integrator/merger selection ignores node death/leave → placeholder rounds forever. (e) **P1** `evaluateRound` reentrancy (no idempotency stamp; concurrent reflect/leave/stall triggers) → double integrations + orphaned barriers. (f) **P1** reflections aren't authenticated against session membership → a zombie's late reflection trips the current barrier and displaces a real node's submission. (g) **P1** the D11 guard is a denylist of the single string `'ollama'` — the `shell` provider (raw bash, no LLM) passes and auto-votes converged/1.0; any custom provider passes; `task.llm_provider` is caller-controlled. (h) **P1** one non-compliant node poisons the mesh — D11 refusal calls `mesh.tasks.fail` from non-owner nodes and `handleFail` has no ownership check. (i) **P2** blind-put-after-CAS lost updates in the new modes, including one introduced by the same commit (70be464) that fixed another instance — and this is the 07-10 audit's deferred evaluateRound race, deferred because "circling doesn't use that path", then built upon. (j) **P2** parse-retry ×3 is circling-only; new modes count parse_failed toward barriers. (k) **P2** cooperative is integrate-with-one-round-lag (not propose-all/integrate-one); rounds_target uncapped by max_rounds. (l) **P2** the modes' deliverable lives in a field whose own prompt says "1-2 sentences" while each node race-merges its own worktree to main at session end. · **HIGH** · gate Phase-1 on (a)-(d) before any multi-machine deploy.

  - 2026-07-16 · **CI federation census cannot fail CI (dead forcing function)** · .github/workflows/test.yml + test/mesh-skip-census.test.mjs · the workflow installs no nats-server and never sets `OPENCLAW_REQUIRE_MESH` (the hard-fail switch exists only inside the census test file — dead config), so all 5 nats-gated federation suites skip as a visible-but-green skip on every run; the marker-drift hard-fail is regex-evadable (non-"nats" skip var, imperative t.skip(), or the canonical string in any comment). WHY: 6.4's contract is "CI-guarded, honestly"; a census that never fails is a green light wired to nothing. · **MEDIUM-HIGH** · 6.4 runtime half / CI config.

  - 2026-07-16 · **Multi-machine NATS security regressed against this plan's own ledger** · services/nats/nats-cluster-node.conf (untracked, step-1.5 enabler) · the template binds `0.0.0.0` on client/monitor/**cluster** ports and the cluster route carries **no authentication** (NATS cluster user/password is unused; only a comment gates it) — while D2/D4 explicitly rejected tailnet-only trust (exec-channel finding #84) and committed to "bind the Tailscale interface as a deliberate, tested change — never all-interfaces." WHY: on a roaming laptop this exposes JetStream (memory events included) + a task-execution channel to any network peer that can reach 6222; and D10's own process rule says architecture-defining choices must be ledgered. · **HIGH** · before a second machine joins: per-machine tailscale-IP binds for cluster+monitor, cluster-block credentials, rotate `OPENCLAW_NATS_TOKEN` (it also echoed into session transcripts during diagnostics), and a DECISIONS threat-model entry.

  - 2026-07-16 · **Chaos harness has vacuous-PASS modes and low evidentiary value** · bin/fed-chaos.mjs · only 3 of 8 T5 cells exist and each has a concrete vacuous-PASS: C5 injects no duplicate (0==0 passes), C7 swallows a failed launchctl restart into a happy-path pass and accepts `aborted`, C1's kill timing is unverified and its recorded PASS depended on a manual daemon-budget tweak the header claims the harness performs but nothing in the file does (not reproducible from the repo). The harness `pkill -9`s every mesh-agent on the machine at each cell start and never restores. C2/C3's "needs R=3 cluster" blocker was stale the evening it was committed (the cluster exists). WHY: 3.5 is the Phase-1 resilience gate; PASS currently certifies little beyond happy-path liveness, FAIL is the only trustworthy direction. · **MEDIUM** · step 3.5 gate; write the 5 missing cells, make cells verify their injections + assert log/KV failure evidence.

  - 2026-07-16 · **Evidence/protocol mechanics dropped across the 07-15 close batch** · all 10 commits since the 2.4 close (6908c46→e6b05bb) · zero mandatory `Runtime-Evidence:` trailers (every close v0.1–v2.3 had one); every 07-15 "AUDIT_PRE" was written the same minute as its POST (post-hoc, defeating the intent/design/risk purpose); 3.1/3.2/3.3/3.4 closed `[x]` with NO AUDIT_PRE at all (PROTOCOL §10 violation); COMPONENT_REGISTRY untouched since 07-11 AND structurally excluded — every 07-15 scope batch omits it from its files block, so the hook would have blocked the Phase-9 update; VERSION carrier broken (three simultaneous `[A]` steps; `v3.5-pre` overwritten by `v6.3`, destroying the resume pointer). `plan-lint` grades all of this CONFORMANT (17 PASS/3 WARN) — its audit check is count-based and its VERSION check shallow (it did flag the missing trailers as one WARN). WHY: for a plan whose identity is "runtime evidence or it didn't happen", these are the mechanisms that make "observed" auditable; without them every close rests on narrative trust. · **HIGH** · restore trailers (the MASTER_PLAN §6.3 pre-commit hook), put COMPONENT_REGISTRY in every batch, one open scope batch at a time, PREs before implementation, teach plan-lint to FAIL (not WARN) on missing per-step PREs + absent trailers.

  - 2026-07-16 · **Step 2.6 (the premise gate) closed `[x]` on ~20% of its contracted sample** · audits/step26_premise-benchmark + docs/PREMISE_BENCHMARK.md · the Verify contract (D3: the gate that decides whether Phases 2–3 exist) requires operator blind-scoring of ≥5 tasks on a pre-agreed rubric, majority win, cost delta recorded. Actual: 1 task blind-scored; rubric defined but never applied (single binary A/B pick, no dimension scores); cost delta unrecorded ("~4-6×" estimate); arms not comparable (grappe arm had repo/file access AND ran after commit 19f0a02 had already landed the F1/F2/F4 fixes — its "winning" artifact opens "all three fixes are already present" — while the solo arm was a tool-less text call against the stale defect brief); one of the two "blind" scorers was the orchestrator. The "3 then 2" scope reduction was attributed to an in-session operator directive that appears NOWHERE in DECISIONS (violating D10's own process rule), the INVENTORY Verify note still demands ≥5, and the row flipped `[ ]→[x]` with no close note. WHY: this is the plan's designed kill-switch; closing it soft means Phases 2–3 stand on an unproven premise. · **HIGH** · reopen 2.6 — either run the contracted ≥5-task benchmark with comparable arms (same tools, same brief-freshness, independent scorers), or ledger a consciously reduced contract over the operator's signature as a DECISIONS entry.

  - 2026-07-16 · **Step 6.3 closed on inference + a hand-fired popup instead of the contracted observation** · audits/step63_fed_probes · the Verify contract requires "a killed grappe member flips a probe and fires a ledgered popup, observed" — no grappe existed, no member was killed; the audit substitutes "a literal kill produces the same edge" (inference), the popup was hand-fired via `openclaw-notify --source grappe`, the BROKEN came from a different probe (stalled session), and one of the four probe verdicts in the close evidence was a bug artifact (varz `.body`, fixed 15 min later in 6.4 — 6.3 never reopened). · **MEDIUM-HIGH** · reopen for a literal member-kill observed in the LIVE watcher (which also requires the watcher to be restarted onto the fed-probe code first).

  - 2026-07-16 · **North-star execution order inverted + planning-doc sprawl** · ROADMAP/IMPLEMENTATION_PHASES/GRANULAR_PHASE1/PHASE1-3_TASKS/INVENTORY · the operator's pinned sequence is (1) harness → (2) wire one OpenClaw to another across machines → (3) grappes → (4) management, yet step (2) has no first-class step anywhere (7.1 `[D]` "until a second machine exists"), so when (2)-shaped work finally happened (the 07-15 multi-machine enabler) it had to enter sideways through a `[D]` row; meanwhile the six planning docs already disagree (D11 banner vs body, "needs R=3" constraints hours after the cluster existed, 3.1's row says `architecture` after its audit renamed it `mode`). Two structural gaps this exposes: gate steps aren't made harder to close than normal steps (2.6/6.3 above), and deferrals conditioned on "X doesn't use Y" have no re-trigger when X changes (the evaluateRound race). WHY: a plan whose spine skips its own step (2) will keep forcing spine work through side doors; disagreeing docs generate the drift the protocol exists to stop. · **MEDIUM** · next planning pass: give the OpenClaw↔OpenClaw-across-machines spine explicit steps (retro-ledger the enabler), collapse redundant planning docs into INVENTORY's Needs/Feeds/Verify, add a "weakening a gate's Verify contract requires a DECISIONS entry first" rule, and record deferral re-trigger conditions next to each deferred item.

  - 2026-07-16 · **Forcing-function erosion** · .claude/hooks/scope-check.sh + repo root · ~9 open scope batches union to a ~76-path allow-list (the hook barely constrains); `.codex/` (a second agent harness with its own hooks, untracked, June 26) writes outside the hook entirely; and the hook over-blocks in the other direction — it denied a write to the session scratchpad OUTSIDE the repo tree, so its path matching is not scoped to the repo. WHY: the one-scope-per-session discipline (MASTER_PLAN §4.2) is the structural anti-drift mechanism; a 76-path allow-list and an ungated second harness both hollow it out. · **LOW-MEDIUM** · close stale scope batches; gitignore + decide the status of `.codex/`; scope the hook's path check to the repo tree.

  - 2026-07-16 · **Review stragglers not covered by the parent entries (same six-source review)** · bin/mesh-task-daemon.js + bin/node-watch.mjs + audits/ · (1) `preferred_mode` is honored on only one of the two task-creation paths, and an unknown mode value throws AFTER the task is persisted → orphaned task debris in KV with no session attached. (2) node-watch BROKEN→OFF transitions never notify — deliberately stopping a unit silently retires its standing BROKEN finding with no ledger trace. (3) 3.5's ≥12h soak cannot start as specified: no feeder script exists anywhere in the repo and the federation-tick plist is unloaded. (4) hygiene: `audits/step26_premise_benchmark/` is an empty duplicate of `step26_premise-benchmark/`; mode-bit + package-lock churn sits uncommitted in the working tree. · **LOW-MEDIUM** · (1) with the Block-3 P1 batch; (2) node-watch honesty pass; (3) 3.5 gate prep; (4) hygiene sweep.

- 2026-07-11 · **OPERATOR REQUIREMENT** (elevated to first-class): the whole federation — all three worker modes + the grappe runtime — must be **implementable out-of-the-box** when the openclaw-node bundle is installed on another machine. · install.sh + services/service-manifest.json + services/launchd|systemd + lib/llm-providers.js · **Wiring audit 2026-07-11 — PARTIAL, three real gaps:** ✅ present: service-manifest registers the mesh daemons (task-daemon/bridge/health-publisher autostart=true, agent=false), launchd+systemd templates exist, install.sh renders+installs them from the manifest, `openclaw-grappe` is in package.json bin. ❌ **Gap A — LLM backend unwired:** `lib/llm-providers` defaults to `claude` (the CLI, needs auth); nothing sets `MESH_LLM_PROVIDER`/`LLM_MODEL` in openclaw.env, install.sh, or the unit templates — so a fresh node's agents would NOT use the local model (ollama/qwen3) the consumer-HW deployment intends. ❌ **Gap B — no working grappe on install:** install only starts services with `--enable-services` (else "load manually"); mesh-agent is autostart=false; per-node agent spawning (spawn-node → per-node units) + node-identity provisioning are absent from install (deferred to Block 6.1). Net: a fresh install stands up NO running grappe — exactly the 2026-07-11 step-2.4 block (task-daemon + agents not running; sandboxed tick can't launch them). ❌ **Gap C — modes incomplete:** only adversarial (circling) has code; cooperative/collaborative are Block 3, unbuilt — so "all three out-of-box" is impossible until Block 3 lands. · **HIGH** · **Action:** (1) reframe Block 6.1 from "fresh node can join a grappe by token" to a full deployability gate (NATS + daemons + per-node agents + LLM-backend config + identity, all install-established and start-verified); (2) **pull the daemon/agent/LLM service-wiring EARLIER** — it's needed for Block 2's own real runs, not just the fleet story; (3) add a runtime check that a clean install stands up a working single-node grappe (a session runs) before the plan calls federation deployable.

- 2026-07-17 · **Security review 2 — queued remainder** (P0/P1s fixed + verified in audits/security_review_2): (a) MC auth middleware — 38 mutation routes trust the loopback bind alone; design token auth like inject-server's. (b) MC's 2 moderate vulns need a BREAKING Next bump (audit fix --force) — do deliberately with build+visual verify. (c) CI quality gates (review F7): lint cleanup first (106 errors/53 warnings in MC), THEN gate lint + MC build + installer --dry-run + npm audit + packed-tarball smoke. (d) Reviewer optimizations: consolidate live (1,766-line) vs dormant (284-line) memory daemons; split install.sh into render/install/service modules; self-host Geist fonts; replace operator-specific .mcp.json absolute paths. (e) embed-benchmark test is load-sensitive (fails at load-avg 18 on this VM; 1334ms vs 500ms idle target) — either budget-per-environment or mark as perf-bench not gate. · **MED**

- 2026-07-17 · **package.json "files" must ship scripts/install/ or the npm-tarball install path is DEAD after the installer split** · package.json:14-37 · structural cleanup 4/4 (this batch) split install.sh into 9 sourced modules under scripts/install/; `npm pack --dry-run` (observed) packs install.sh (73.9kB) but nothing under scripts/ — cli.js execs the packed install.sh, which now `source`s scripts/install/*.sh → the npx/tarball flow fails at first source while the git-clone flow works. package.json is outside the structural-cleanups files block per the agent brief, so not edited; the fix is one array element in "files": `"scripts/install/",` (e.g. after `"openclaw.env.example",`). · **HIGH (release-blocking for the npm path; inert for git-clone installs)** · add before the next npm publish / tarball smoke test. **RESOLVED same day:** orchestrator added package.json to the structural-cleanups batch and inserted `"scripts/install/",` into files; `npm pack --dry-run` observed packing all 9 modules + install.sh.

- 2026-07-17 · **install.sh --dry-run still performs real side effects in five pre-existing unguarded sites** (observed during the split's before/after baseline runs; predates the split, byte-identical behavior after it) · scripts/install/components.sh (harness-sync apply runs live merge; hyperagent status executes), scripts/install/services.sh (build-notifier-app.sh + build-launcher-app.sh rebuild real app bundles at ~/.openclaw/share + ~/Applications), scripts/install/integrations.sh (settings.json jq-merge rewrites the live workspace file; .claude/hooks/*.sh recopied) · all were content-idempotent on this node (settings.json + harness-rules.json diffed identical pre/post; the 3 F4-guarded unit-render sites + generate_config outputs verified untouched via -newer marker) but they mutate mtimes and rebuild bundles on a box the flag promises not to touch — the "--dry-run honesty" item already queued in the 07-11 triage, now with an exact site list. · **LOW-MEDIUM** · fold into the queued dry-run-honesty pass; the sites are now grep-isolated per module.

- 2026-07-17 · **bin/openclaw-status.mjs still introspects the deleted bin/openclaw-memory-daemon.mjs** · bin/openclaw-status.mjs:17,32 · the daemon consolidation (structural-cleanups batch: federation wiring folded into workspace-bin/memory-daemon.mjs behind OPENCLAW_FEDERATION=1, dormant file deleted) leaves this diagnostic CLI reading DAEMON_PATH = bin/openclaw-memory-daemon.mjs; readOrEmpty() means it does not crash but every daemon-side row (NATS connect, graphCache, subscriber, extraction trigger, consolidation scheduler, event log) now reports NOT_WIRED against an empty string — a false-red status tool. The file is in NO active scope batch, so the fix was captured here instead of worked around (agent brief: stop-and-report on hook blocks). Fix is small: point DAEMON_PATH at workspace-bin/memory-daemon.mjs and re-check the row expectations (subscriber/scheduler rows now sit behind the OPENCLAW_FEDERATION gate; createSessionStore/createMemoryBudget rows never matched the live daemon's real factory names — `new SessionStore()` / `createBudget`). · **LOW** · 15 min once scoped; until then treat openclaw-status output as stale. **RESOLVED same day:** orchestrator added bin/openclaw-status.mjs to the structural-cleanups batch and repointed DAEMON_PATH + fixed all three alias false-negatives (natsConnect / createBudget / new SessionStore); observed output 13 wired / 1 stub (Block 11) / 0 not-wired, exit 0.

- 2026-07-18 · **extraction quality: confabulation on recursive tails** · lib/extraction-prompt.mjs + qwen3:8b · the stall fix (audits/extraction_stall) restored the mechanical pipeline, but the verification flush extracted an invented Alice/Bob/Kafka decision narrative from a tail that quoted test snippets — sessions ABOUT extraction are the model's worst case; historical normal-content runs produced 17–28 real facts. Candidate mitigations (untested): literal-presence instruction in SYSTEM_PROMPT; bigger model tier (RAM call); post-extraction grounding check (reject entities whose name never appears in the tail) · **MED** (pollutes the durable store slowly; 4 junk entities left to decay) · quality pass after the queue's infra items.

- 2026-07-18 · **NATS bus flaps on this VM** · nats-server 2.12.6 :4222 + all clients · observed in one afternoon: NATS_PROTOCOL_ERR 13:06, AUTHENTICATION_TIMEOUT 14:14, disconnect/reconnect cycles 13:37/15:45, and one published extract_request (13:35:25) that never reached the daemon's live subscription (delivery unproven either way — connz not captured at the time). Coalescing + reconnect handled the rest, but fire-and-forget triggers published into a flap window are silently lost — anything that MUST fire should either use JetStream or receipt-check (the extract trigger is fire-and-forget by design) · **MED** · fold into queue item on NATS client auth/stability.

- 2026-07-18 · **knowledge-index re-embeds entire sessions on every growth cycle** · lib/mcp-knowledge/core.mjs indexSessionTurns + workspace-bin/knowledge-index-job.mjs · the hash-change path is delete+reinsert for ALL chunks — this session costs ~850 chunks ≈ 20–28 min of embedding per cycle, every 10-min throttle window while a session is active, so the knowledge index perpetually lags the live session and the box carries constant embed load. Post item-2 this burns a child process instead of deafening :7893, but the cost is unchanged. Needs chunk-level incremental indexing (embed only new/changed chunks) · **MED** · pair with the embedding/model-tier hardware decision.

- 2026-07-18 · **Ollama runner VRAM degradation after watchdog kills** · Ollama 0.32.1 + qwen3:8b on the 19.5GB VM · after grammar-stall watchdog kills, the auto-reloaded model can land mostly off-GPU (observed 0.6GB size_vram, 1.1 tok/s vs 5.3GB / ~2.5+ tok/s healthy) and stays degraded until `ollama stop` + reload. With the stall fixed the kill trigger is gone, but the pattern will recur on any future runner death. Durable mitigations are RAM-budget tradeoffs: keep_alive in the daemon's LLM calls (pins 5.3GB resident), or a watcher probe that greps size_vram and alerts · **LOW-MED** · operator hardware call; pair with the model-tier decision.

- 2026-09-19 · **the federation silo fails two of plan-lint's surface conformance bars and has since at least the 2026-08 governance recovery** · memory-plan/plans/federation/ (automation.json absent; tick-logs/ absent) · `plan-lint.sh federation` reports both as FAIL, so the silo grades NONCONFORMANT regardless of the content work done in it. Per PROTOCOL §10 these are the automation and history surfaces: without the first the viewer's Automation tab has no config to render and the generic tick engine has nothing to point at; without the second ticks have nowhere to write, so the Live/Progress/History tabs are dead. Neither gap affects the plan's correctness — federation is BLOCKED and its chain is meant to short-circuit — but they mean the silo cannot be handed to the tick chain if the 2.8 benchmark ever unblocks it, and a NONCONFORMANT grade masks any *new* conformance regression behind two permanent reds. Observed while linting the pipeline-mode design batch; both predate it and are outside its files block. · **LOW now, MEDIUM the moment the plan unblocks** · whoever scopes 2.7/2.8 execution, since that is when the chain would need to run.

- 2026-09-19 · **plan-lint reads the LAST `**Set at:**` line in a SCOPE.md, so retained scope history silently misdates the live scope** · workspace-bin/plan-lint.sh:64 · the field is extracted with `grep -E '^\*\*Set at:\*\*' | tail -1`. A silo that keeps prior scopes in the same file — which the federation and protocol SCOPE.md files both do, and which the repo treats as good practice — has its live scope graded against the oldest retained date. Observed directly: a scope set the same day graded "47d old (>30) — re-set or retire it" until the historical line was de-bolded. The failure is silent and inverted (it accuses a fresh scope of being stale), and it will recur in any silo that retains history in the conventional way. The same `tail -1`/`head -1` extraction shape is worth checking on the other scope fields. · **MEDIUM** · whoever next touches plan-lint's scope-hygiene checks; note the de-bolding now in federation/SCOPE.md is a local workaround, not a fix.

- 2026-09-21 · **`handleCirclingStepTimeout` deletes its timer-map entry without clearing the timer** · bin/mesh-task-daemon.js (`circlingStepTimers.delete(sessionId)` at the top of the handler) · correct when the timer itself is the caller (already fired), but any direct invocation — the 60 s rehydration sweep in a race with a still-armed timer, a future test surface, an operator tool — leaves a live 10-minute `setTimeout` that no code path can reach again, and whose late firing re-enters the timeout logic against a step that may already have advanced (the snapshot guard makes that a no-op, but the orphan keeps the process alive and the log noisy). Found at step 2.7 because the pipeline twin had the identical shape and kept a test child alive at 0 % CPU after all tests passed; the pipeline handler now clears (idempotent) instead of deleting, the circling one is outside 2.7's files block. · **LOW** (production reaches it only through the sweep-vs-timer race) · whoever next scopes circling code — one-line change, same as the pipeline fix.
