# openviking-adopt — deploy + runtime probes (2026-09-21_110331)

repo `claude/funny-mccarthy-7erfg3` @ `34c8687` · host `MoltyMacs-Virtual-Machine.local` · workspace `/Users/moltymac/.openclaw/workspace`

| Step | Result | Evidence |
|---|---|---|
| daemon | PASS | :7893 /health ok (pid 69466) |
| 3.1 | PASS | state.db user_version=6, entity_aliases + decisions.superseded_by present |
| 3.2 | PENDING | no live flush yet since deploy: entity_aliases=0 superseded=0 (entities=18). Re-run --probe after the next session flush (idle 15 min / session end). |
| 2.1 | PASS | directory_summaries=25; knowledge_tree depth 2 → 9 rows, all with abstracts |
| 2.2 | FAIL | directories_llm=0, re-pass built 0, llm calls 1 |
| 1.1 | PASS | path_prefix=.learnings/ → 8 hits all inside; path_prefix=memory/ → 8 inside; unscoped → 12 hits spanning directories; hits carry dir_abstract=true |
| 4.1 | FAIL | plugins.slots.contextEngine='' (expected openclaw-node-memory) |

PASS rows are the Runtime-Evidence for closing the matching INVENTORY row (PROTOCOL §3 Phase 9).
PENDING rows need a later `--probe` run (3.2 after a live flush) or an operator action.
