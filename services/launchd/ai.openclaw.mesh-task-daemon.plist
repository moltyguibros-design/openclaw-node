<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>Label</key>
    <string>ai.openclaw.mesh-task-daemon</string>
    <key>Comment</key>
    <string>OpenClaw Mesh Task Daemon — NATS task coordinator</string>
    <key>ProgramArguments</key>
    <array>
      <string>${NODE_BIN}</string>
      <string>${OPENCLAW_REPO_DIR}/bin/mesh-task-daemon.js</string>
    </array>
    <key>KeepAlive</key>
    <true/>
    <key>RunAtLoad</key>
    <true/>
    <key>StandardOutPath</key>
    <string>${OPENCLAW_WORKSPACE}/.tmp/mesh-task-daemon.log</string>
    <key>StandardErrorPath</key>
    <string>${OPENCLAW_WORKSPACE}/.tmp/mesh-task-daemon.err</string>
    <key>EnvironmentVariables</key>
    <dict>
      <key>HOME</key>
      <string>${HOME}</string>
      <key>PATH</key>
      <string>${OPENCLAW_WORKSPACE}/.npm-global/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
      <key>NODE_PATH</key>
      <string>${OPENCLAW_REPO_DIR}/node_modules:${OPENCLAW_REPO_DIR}/lib</string>
      <!-- Local-first: the mesh bus is loopback-only. -->
      <key>OPENCLAW_NATS</key>
      <string>${OPENCLAW_NATS}</string>
      <key>OPENCLAW_REPO_DIR</key>
      <string>${OPENCLAW_REPO_DIR}</string>
      <!-- Circling step budget: an OpenClaw (advanced-LLM) worker's finalization
           turn can exceed the 10-min code default; 30 min bounds a real hang
           without aborting a healthy session (2.4 finalization abort, 2026-07-15). -->
      <key>MESH_CIRCLING_STEP_TIMEOUT_MS</key>
      <string>1800000</string>
    </dict>
    <key>ThrottleInterval</key>
    <integer>10</integer>
  </dict>
</plist>
